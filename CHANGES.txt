v2.0.10
- Bug fix for v2.0.9 not showing in 007revad package source in Package Center.
- Bug fix for v2.0.9 armv7, armv8 and i686 packages having noarch in their INFO file.

v2.0.9
- Package adds itself to DSM 7's "Control Panel > Application Permissions" with only Administrators allowed to see or run the package.
  - You can the permissions in "Control Panel > Application Permissions".

v2.0.8
- Changed to no longer need the user to set elevated permissions.
  - Installation deletes the old sudoers file if it exists.
- Settings window now shows the schedule as disabled if the user deleted the schedule from Task Scheduler instead of assuming the schedule was still enabled.
- Changed to be secure against possible exploits.

v1.0.5
- Bug fix for setting log schedule's first run at midnight.

v1.0.4
- First release.
